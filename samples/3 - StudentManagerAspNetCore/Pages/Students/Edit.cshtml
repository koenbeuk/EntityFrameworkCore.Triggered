@page
@model StudentManager.Pages.Students.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Student</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Student.Id" />
            <div class="form-group">
                <label asp-for="Student.DisplayName" class="control-label"></label>
                <input asp-for="Student.DisplayName" class="form-control" />
                <span asp-validation-for="Student.DisplayName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Student.RegistrationDate" class="control-label"></label>
                <input asp-for="Student.RegistrationDate" class="form-control" />
                <span asp-validation-for="Student.RegistrationDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
